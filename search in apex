
to find string where use in list of apex

String searchString = 'getData';
List<ApexClass> matchingClasses = [SELECT Name, Body FROM ApexClass ORDER BY CreatedDate DESC];
for (ApexClass ac : matchingClasses) {
    if (ac.Body.contains(searchString)) {
        List<Integer> lineNums = new List<Integer>();
        Integer index = ac.Body.indexOf(searchString);
        while (index != -1) {
            Integer lineNum = ac.Body.substring(0, index).split('\n').size();
            lineNums.add(lineNum);
            index = ac.Body.indexOf(searchString, index + searchString.length());
        }
        System.debug('Seach Key:-'+searchString+'->Class Name: ' + ac.Name + ', Line Numbers: ' + lineNums);
    }
}



dynamic field name in object 
List<String> dumyList = new list<string>{'Name','website'};
 System.debug('dumyList'+dumyList);   
List<account> listAcc = new List<account>();     
account acc = new account();
for(integer i=0;i<dumyList.size();i++){
    listAcc.clear();
    string ap = dumyList[i];
    System.debug('list'+ap);
    acc.put(ap, 'nishar003');
    //listAcc.add(acc);
}
System.debug('listAcc'+listAcc);
insert acc;